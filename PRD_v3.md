# TripStudio — 产品说明书 v3

> 版本：v3.0（2026-05-27）
> 作者：刘星
> 取代：PRD_v2.md（v2.2 的"路书工具"叙事整体作废）

---

## 这是什么

你定好了一段旅行，距离出发还有几周或几个月。这段时间你会反复打开行程，幻想自己会遇到什么——但目前没有任何产品认真服务这段"行前期待期"。

TripStudio 是给这段时间用的。你把行程录进来，它替你把这段旅行**先走一遍**，把过程中"会遇到的痕迹"——一张张明信片——按日期一张一张投递回来给你。

它不是路书工具。它是**为一段还没发生的旅行提前生成的一份遗物**。

它的核心动作不是"分享出去"，是"保存下来"。这是一份私人物件，主要给自己，其次给最亲近的 1–2 个人。

---

## 为什么是现在做这个

市面上的"行程整理工具"已经被圆周旅记、马蜂窝等占满，背靠内容生态有结构性优势，做工具线毫无胜算。

但行前期待期被所有产品忽略。这是一个**情感时刻**而不是工具时刻。情感时刻拼的是调性、节奏、克制——小团队反而占优。旅行青蛙当年是 4 个人做的。

TripStudio 要做的是把这段时间从"死区"变成一个**值得保存的过程**。

---

## 谁会用它（v1.0 只服务一类人）

**为自己/小团旅行的录入者**。已经定好了旅行（机票酒店都有了），距离出发还有一段时间，会主动在出发前打开 app 看明信片。

v1.0 砍掉的目标用户：
- 内容创作者（要发小红书的）—— 等 v1.2+ 的分享导出
- 精品行程销售方（B 端）—— 完全不在产品定位内
- 旅行回来后做总结的人 —— v1.0 只服务"出发前"，"回来后"是另一个产品

---

## 核心交付物（双面）

录入完成 → "保存为作品" → 得到两份东西：

### 一、行程本（即时可见）
- 列表视图：所有 event 按日期/手动顺序排列
- 地图视图：所有锚点 + 连线
- 这部分本质上就是当前已实现的 v2 形态，是用户**当下能看的**理性视图

### 二、明信片集（按日期解锁）
- 保存的瞬间，**全部明信片一次性生成完毕**
- 但 UI 默认只显示"今天及之前"的明信片
- 后续的明信片显示为"5 月 8 日解锁"这种锁态
- 用户可点"偷看后续"打破节奏（保留好奇心出口）
- 出发日之后每天到日子自动解锁当天那张

**两个交付物的关系**：行程本是"我计划好的事实"，明信片集是"这趟旅行还没发生时已经留下的痕迹"。前者理性，后者情感。

---

## 明信片视觉系统

### 调性三原则

1. **匿名**：明信片没有"角色"在写。没有"小蛙"、没有"我替你"、没有第一人称署名。它就是一张被寄回来的纸。
2. **克制**：留白多于信息，物件感大于解释，图大于字，没有 emoji 旁白和情绪打分。
3. **数据驱动**：所有数字（温度、票价、日落、坐标、距离）都来自真实数据查询，不模糊不编造。

参考气质（不是要做成一样，是同类气质）：
- Moleskine 城市笔记本的留白
- 中田英寿《天空之城》一页一物
- Wes Anderson 道具风的票根/邮戳/胶带拼贴
- 老 Lonely Planet 的手绘地图
- 田中達也的微缩物件感

明确不要的气质：
- 卡通可爱、IP 形象、表情包
- 小红书爆款式的精致摆拍
- "心情打四星" / "feeling great!" 这类替用户表达情绪的元素
- AI 生成味重的山脉照片

### 8 模块 Schema

每张明信片由以下 8 种模块**按需组合**，一张通常用 5–7 个：

| # | 模块 | 内容 | 数据源 |
|---|------|------|--------|
| 1 | **HEADER** | 地名、日期、编号、坐标 | 用户行程 |
| 2 | **STAMP** | 邮戳：地名 + 日期 + 到达/离开 | 预设 SVG 资产 |
| 3 | **HERO** | 主视觉：大字排版 / 手绘地图片段 / SVG 线稿 三选一 | 排版引擎 + SVG 库 |
| 4 | **WEATHER** | 温度、天况、日出、日落 | 真实天气历史/预测 API |
| 5 | **MAP** | 当日路线手绘地图片段 | 行程坐标 + 手绘风样式 |
| 6 | **TICKET** | 票根/收据：交通票、餐厅小票、缆车票 | 行程数据 + 价格库 |
| 7 | **FIELD NOTE** | 田野观察：3–5 行短句 | v1.0 留空或用静态预设；v1.1 接 AI |
| 8 | **TIP** | 一句话提示（卡纸贴片样式） | v1.0 留空或用静态预设；v1.1 接 AI |

**v1.0 的关键约束**：模块 7 和 8 **不调用 AI**。要么留空，要么用基于数据规则触发的静态文案（例："旺季提示"、"日落提前一小时到"等模板）。AI 推迟到 v1.1。

### HERO 槽位 v1.0 不放真实景物图

可选三种之一：
- **大字排版**：地名 + 坐标 + 日期，海报感
- **手绘地图片段**：当日小区域路线
- **SVG 线稿**：一栋建筑、一座山、一条街，从一个预设库里挑

完全不做 AI 图像生成。理由：成本高、调性不稳、AI 味儿是产品最大风险。

---

## v1.0 范围

### 必须做（"值得 share 给 peer"的下限）

**录入侧**（基本沿用现有 v2 实现）：
- 五类 event 卡片（酒店/游览/餐饮/活动/交通）
- 名称 + 地点 + 日期 + 标签
- 地点字段：搜索 + 地图浮窗，三态锚定（precise / manual / none）
- 时间字段：选填 YYYY-MM-DD，支持范围
- 拖拽排序（仅无日期组）
- 卡片折叠/展开

**多 trip 管理**（新增）：
- 首页改为"我的旅行"列表
- 新建 / 打开 / 删除 / 重命名 trip
- 每个 trip 独立 localStorage 存储

**保存仪式**（核心新增）：
- 录入完成后有一个明确的"保存为作品"动作
- 保存触发明信片集生成（所有日期一次性生成）
- 保存后进入"作品视图"：行程本 + 明信片集双面呈现

**明信片集**（核心新增）：
- 8 模块渲染引擎（HTML/CSS）
- 真实天气数据接入（每张明信片用当日真实历史/预测温度）
- 真实日落/日出计算
- 手绘风地图渲染
- 邮戳 SVG 资产库（先做 5–10 个常用国家/地区）
- 解锁机制：默认只显示当天及之前，未来日期显示锁态

**导出兜底**（用户能 share 出去的关键路径）：
- 单张明信片可截图保存
- 整个作品可导出 PDF（按日排版的明信片合辑）
- 导出 JSON（保险）

### 必须不做

| 砍掉 | 原因 |
|------|------|
| AI 文本生成 | 推迟到 v1.1，v1.0 用静态/模板填 |
| AI 图像生成 | 推迟到 v1.2 或永远不做 |
| 分享链接 / URL 序列化 | v1.2 |
| 多人协作 | 不在路线图 |
| 账号体系 / 云同步 | 不在路线图 |
| 移动端原生适配 | v1.0 desktop-first，移动端响应式做最低限即可 |
| 内容创作者用的"小红书图集导出" | v1.2 |
| 国际化 | 中文版做透 |
| iframe srcdoc 渲染 | 旧 PRD v2 已删，确认不复活 |
| 主题/章节/复杂住宿推导 | 旧代码里的 themeKey / buildChapters / buildOvernight 全部移除 |

### v1.0 验收标准（北极星）

> **v1.0 完成 = 我（第一个用户）把自己真实的下一段旅行录入完成、保存，得到的明信片集让我愿意发给我的同行人或最好的朋友。**

这是一个行为标准，不是感觉标准。如果保存完只想自己看不想发，v1.0 没完成；如果保存完会主动发给 1–2 个最近的人，v1.0 闭环成立。

---

## v1.1 路线（AI 探路真正接入）

v1.0 跑通"形式"之后，v1.1 才接 AI 进 FIELD NOTE 和 TIP 两个模块。

- AI 输入：当日行程 + 实时数据（天气、节假日、当地新闻、营业时间）
- AI 输出严格 JSON：`{ field_note: string[3-5], tip: string }`
- 总输出限制 60 字以内
- Grounding 数据是产品本体，不允许 AI 凭空生成数字

为什么不放 v1.0：
- v1.0 真正的赌局是"明信片这个形式用户喜不喜欢"，不是"AI 写得好不好"
- 没有 AI 也能做出 80% 体验
- AI 一上就要 API key / 后端 / 成本 / 幻觉 / 节流五件事一起做，工作量翻倍
- 从"好看的明信片"升级到"会说话的明信片"，升级感本身就是产品节奏

---

## v1.2+ 路线（不承诺时间）

- 分享链接（行程数据序列化到 URL hash）
- 完整 PDF 排版优化
- 小红书图集尺寸导出（首次面向"内容创作者"用户）
- AI 文本解析录入（粘贴备忘录自动拆 event）
- 路书主题（2–3 套视觉风格）
- 推送 / 邮件触发的真实定时投递（v1.0 是"UI 上按日期解锁"的伪定时）

---

## 明确不做

| 不做 | 原因 |
|------|------|
| 路线规划 / 导航 | 不替用户决策，交通 event 只渲染 from→to |
| 景点 / 餐厅推荐 | 不替用户决策 |
| 多人协作 | 后端工作量过大 |
| 账号体系 | localStorage + 导出 JSON 已够 |
| AI 图像生成 | 调性风险大于收益 |
| "AI 角色" / 卡通 IP | 与匿名调性冲突 |

---

## 从 v2 到 v3 的代码影响（高层）

v2 PRD 已经做对的事（保留）：
- 三态锚定 + 表单内地图浮窗
- OSRM 懒加载路径
- 重叠点微偏移
- localStorage 持久化
- 触控板 ctrl+wheel 缩放

v3 需要在 v2 之上新增/重构：
- 模块化代码（拆 ES module，去掉 2228 行单文件）
- 多 trip 数据层（首页改造）
- 保存仪式 + "作品视图"
- 8 模块明信片渲染引擎
- 真实数据接入层（天气/日落 API）
- 邮戳 SVG / 手绘地图样式 / 静态预设文案库
- PDF 导出

具体改造路线由独立的 ROADMAP 文档承接（见 task #3）。

---

## 待解决的问题（不阻塞 PRD 定稿）

1. **天气 API 选型**：OpenWeather 历史天气需付费 API key，如何在零后端 + 零密钥泄漏前提下接入？候选方案：用户自带 key / 公开免费源（Open-Meteo 历史接口免费可用）/ v1.0 先用气候平均值兜底
2. **邮戳/手绘地图素材的产出方式**：自己画 / 找设计 / 找开源 SVG 库 / 程序生成？
3. **静态预设文案库的覆盖度**：v1.0 没有 AI，FIELD NOTE 和 TIP 是留白还是有低限度的规则触发？建议 v1.0 直接留白（让模块"缺一点"反而更克制），v1.1 再补 AI

---

*文档创建：2026-05-27 v3.0*
*取代：PRD_v2.md*
